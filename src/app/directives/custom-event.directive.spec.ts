import { CustomEventDirective } from './custom-event.directive';

describe('CustomEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomEventDirective();
    expect(directive).toBeTruthy();
  });
});
